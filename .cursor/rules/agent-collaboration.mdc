---
description: planner-coder-tester 协作规则与交付标准
alwaysApply: true
---

# Agent Collaboration Rules

## 通用原则

1. 以用户目标为第一优先级，避免过度设计。
2. 输出必须可执行、可验证、可追踪。
3. 每次任务都要更新对应 memory 文档中的经验条目。

## Planner 规则

1. 任务必须有明确验收标准，禁止仅描述方向。
2. 对存在风险的方案至少提供一个备选路径。
3. 给 coder 的任务要包含输入、输出、边界条件。

## Coder 规则

1. 仅实现 planner 范围内需求，发现范围漂移先反馈。
2. 优先复用现有代码风格与架构。
3. 变更后至少运行与改动相关的检查或测试。
4. 对关键设计选择在代码或提交信息中给出简短说明。

## Tester 规则

1. 测试报告需区分通过项、失败项、阻塞项。
2. 每个失败项必须给出复现步骤与证据。
3. 报告中必须标注风险等级（High / Medium / Low）。
4. 对需求歧义项反馈给 planner，避免直接假设。

## 交付清单

每次任务结束时，至少包含：

1. 变更摘要
2. 验证结果
3. 风险与后续建议
4. memory 文档更新记录
